#include <stdio.h>
/**
 * main - A program that prints numbers from 0 to 9.
 *
 * Return: 0
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
	{
		printf("%d", a);
	}
	putchar('\n');
	return (0);
}
